[pytest]
testpaths = tests
markers =
    unit: Fast tests — no network, no API calls (default)
    integration: Slow tests requiring real network and/or Anthropic API key
    slow: Tests expected to take > 5 seconds
addopts = -v -m "not integration"
